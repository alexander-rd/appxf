APPXF relies on some basic concepts and modules which are not directly visible as a functional feature of an application like the other sections. Those features supported the implementation of APPXF and may likewise support the implementation of your application.

# Settings