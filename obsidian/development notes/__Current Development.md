# ToDo
 * finalize the LICENSE on root
	 * check references on how such "dual"/"combined" licensing can be written
 * branch_close: the gh create command (and likely the update may be frozen in a selection or not able to succeed)
	 * script should catch errors, quit executing and printing them


